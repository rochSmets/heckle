
#ifndef WALL
#define WALL


/* _____ manage particles when reaching the walls ___________________________ */
void wall(struct sti *, struct stx *, struct st1 *, struct st2 *, struct stp *[NS+1], int, int, int);

#endif

