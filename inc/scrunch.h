
#ifndef KABOUM
#define KABOUM


/* __ report problem if part is not in the subdomain ________________________ */
void scrunchpart(struct sti,
                 struct stx *,
                 struct st1 *,
                 struct st2 *,
                 struct stp *[NS+1],
                 int,
                 int,
                 char *,
                 int);

#endif

