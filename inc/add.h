
#ifndef ADD
#define ADD


/* ____ add particles when the density is too low ___________________________ */
void add(struct sti *, struct stx *, struct stt *, struct st2 *, struct stp *[NS+1], MPI_Comm);

#endif

